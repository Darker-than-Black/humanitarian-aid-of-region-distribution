import { ElementHostDirective } from './element-host.directive';

describe('ElementHostDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ElementHostDirective();
    expect(directive).toBeTruthy();
  });
});
